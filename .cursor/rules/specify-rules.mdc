---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# agent-context-explorer Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-15

## Active Technologies
- TypeScript (strict) + VS Code Extension API, Mocha, NYC, ESLint, Vite bundling (002-test-coverage)
- N/A (test and tooling changes only) (002-test-coverage)
- TypeScript (strict), Node.js **24.x** for CI and contributor tooling + npm, Mocha, NYC, ESLint, Vite (build), VS Code extension API (003-ci-quality-gate)
- N/A (config files only) (003-ci-quality-gate)
- TypeScript **strict** (project standard), Node **≥24** (see `package.json` / CI). + VS Code Extension API (`vscode`), existing ACE scanners (`VSCodeFsAdapter`, `scan*Core` in `src/scanner/core/`), MCP SDK (stdio server + tools in `src/mcp/`). (004-agents-view-scan)
- N/A (files on disk only; no DB). (004-agents-view-scan)

- TypeScript 5.x (strict) + VS Code Extension API, existing scanners (RulesScanner, CommandsScanner, SkillsScanner, AsdlcArtifactScanner), ProjectManager (001-tree-view-refactor)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x (strict): Follow standard conventions

## Recent Changes
- 004-agents-view-scan: Added TypeScript **strict** (project standard), Node **≥24** (see `package.json` / CI). + VS Code Extension API (`vscode`), existing ACE scanners (`VSCodeFsAdapter`, `scan*Core` in `src/scanner/core/`), MCP SDK (stdio server + tools in `src/mcp/`).
- 003-ci-quality-gate: Added TypeScript (strict), Node.js **24.x** for CI and contributor tooling + npm, Mocha, NYC, ESLint, Vite (build), VS Code extension API
- 002-test-coverage: Added TypeScript (strict) + VS Code Extension API, Mocha, NYC, ESLint, Vite bundling


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
